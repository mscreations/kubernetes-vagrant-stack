# All text files — normalize to LF
* text=auto eol=lf

# Shell scripts — force LF explicitly
*.sh text eol=lf
*.bash text eol=lf

# Python scripts — force LF
*.py text eol=lf

# Ignore binaries for text normalization
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
*.tar.gz binary
*.tgz binary
*.exe binary
*.dll binary
